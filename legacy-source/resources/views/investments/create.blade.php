@extends('layouts.app') @section('content')<h2>Investment Proposal</h2><form method="POST" action="{{ route('investments.store') }}">@csrf<label>Project</label><select name="project_id">@foreach($projects as $p)<option value="{{ $p->id }}">{{ $p->project_id }} - {{ $p->client->full_name }}</option>@endforeach</select><input name="estimated_cost" type="number" step="0.01" placeholder="Estimated cost"><input name="client_initial_contribution" type="number" step="0.01" placeholder="Client contribution"><input name="contractor_investment" type="number" step="0.01" placeholder="Contractor investment"><input name="client_proposed_profit_margin" type="number" step="0.01" placeholder="Client proposed profit %"><textarea name="client_profit_reason" placeholder="Reason"></textarea><input name="repayment_duration_months" type="number" placeholder="Repayment months"><input name="payment_day" placeholder="Payment day"><input name="payment_method" placeholder="Payment method"><button>Submit</button></form>@endsection