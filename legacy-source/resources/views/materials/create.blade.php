@extends('layouts.app') @section('content')<h2>Add Material Responsibility</h2><form method="POST" action="{{ route('materials.store') }}">@csrf<label>Project</label><select name="project_id">@foreach($projects as $p)<option value="{{ $p->id }}">{{ $p->project_id }} - {{ $p->client->full_name }}</option>@endforeach</select><label>Material</label><input name="material_name"><label>Responsibility</label><select name="supplier_responsibility"><option>Client Supplies</option><option>Contractor Supplies</option><option>Shared Supply</option></select><label>Estimated Cost</label><input name="estimated_cost" type="number" step="0.01"><label>Status</label><input name="delivery_status"><label>Required Date</label><input name="required_date" type="date"><textarea name="remarks"></textarea><button>Save</button></form>@endsection