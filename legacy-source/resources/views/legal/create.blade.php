@extends('layouts.app') @section('content')<h2>Create Legal Document</h2><form method="POST" action="{{ route('legal.store') }}">@csrf<label>Project</label><select name="project_id">@foreach($projects as $p)<option value="{{ $p->id }}">{{ $p->project_id }} - {{ $p->client->full_name }}</option>@endforeach</select><input name="document_type" placeholder="Agreement / Default Notice / Final Demand"><input name="document_title" placeholder="Title"><textarea name="document_body" placeholder="Document body"></textarea><input name="legal_officer" placeholder="Legal officer"><select name="legal_status"><option>Draft</option><option>Under Review</option><option>Approved</option><option>Issued</option></select><textarea name="legal_remarks" placeholder="Remarks"></textarea><button>Save</button></form>@endsection