@extends('layouts.app') @section('content')<h2>Procurement Request</h2><form method="POST" action="{{ route('procurement.store') }}">@csrf<label>Project</label><select name="project_id">@foreach($projects as $p)<option value="{{ $p->id }}">{{ $p->project_id }} - {{ $p->client->full_name }}</option>@endforeach</select><input name="item_name" placeholder="Item name"><input name="quantity" type="number" step="0.01" placeholder="Quantity"><input name="unit" placeholder="Unit"><input name="unit_price" type="number" step="0.01" placeholder="Unit price"><select name="procurement_status"><option>Requested</option><option>Approved</option><option>Ordered</option><option>Delivered</option><option>Issued to Site</option></select><textarea name="remarks"></textarea><button>Save</button></form>@endsection