@extends('layouts.app') @section('content')<h2>Record Payment</h2><form method="POST" action="{{ route('payments.store') }}">@csrf<label>Project</label><select name="project_id">@foreach($projects as $p)<option value="{{ $p->id }}">{{ $p->project_id }} - {{ $p->client->full_name }}</option>@endforeach</select><input name="amount_paid" type="number" step="0.01" placeholder="Amount"><input name="payment_date" type="date"><input name="payment_method" placeholder="Method"><input name="reference_number" placeholder="Reference"><textarea name="remarks"></textarea><button>Save</button></form>@endsection